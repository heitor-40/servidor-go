```

### Componentes do backend

| Componente | Pacote | Responsabilidade |
|---|---|---|
| Entrypoint | `cmd/api/main.go` | Ponto de entrada; chama `app.Run()` |
| Compositor | `internal/app/app.go` | Carrega config, abre DB, instancia e conecta todos os pacotes, registra middlewares |
| ConfiguraÃ§Ã£o | `pkg/config` | LÃª variÃ¡veis de ambiente e valida `JWT_SECRET` |
| Middleware | `internal/middleware` | Chain: RequestID â†’ Logger â†’ Recover â†’ CORS |
| AutenticaÃ§Ã£o | `internal/auth` | Login, perfil do usuÃ¡rio, CRUD de usuÃ¡rios, validaÃ§Ã£o de JWT e roles |
| ApÃ³lices | `internal/apolice` | DomÃ­nio principal: CRUD, KPIs, fila de aÃ§Ã£o, documentos, exportaÃ§Ã£o, busca |
| NotificaÃ§Ãµes | `internal/notificacao` | Lista, marca como lida e arquiva notificaÃ§Ãµes |
| Auditoria | `internal/audit` | Lista e registra aÃ§Ãµes em `audit_logs` (JSONB) |
| ConexÃ£o DB | `internal/database/postgres.go` | Abre e valida conexÃ£o PostgreSQL |
| Response | `pkg/response` | Padroniza responses JSON (`Success`, `Fail`) |

---

## NÃ­vel 4: CÃ³digo

O Diagrama de CÃ³digo detalha as classes (structs, interfaces e funÃ§Ãµes) do pacote de maior complexidade de domÃ­nio: **`internal/apolice`**.

### Diagrama de classes

```mermaid
classDiagram
    direction TB

    class Repository {
        <<interface>>
        +List()
        +Get(luc)
        +Create(model)
        +Update(luc, model)
        +Delete(luc)
        +SearchApolices(query)
        +GetCoberturas(luc)
        +GetHistorico(luc)
        +GetHistoricoGlobal(limit)
        +GetAtividadesRecentes(limit)
        +UpdateObservacoes(luc, obs)
        +Renovar(...)
        +UpdateResponsavel(...)
        +GetLojas()
        +GetUsuarios()
        +Documento CRUD
    }

    class Apolice {
        +Luc string
        +Loja string
        +Segmento string
        +Seguradora string
        +Vigencia time.Time
        +Vencimento time.Time
        +Status string
        +Cobertura float64
        +DiasRestantes int
        +Responsavel string
        +ResponsavelID *int64
        +Observacoes string
        +CNPJ string
        +NumeroApolice string
    }

    class Cobertura {
        +ID int
        +ApoliceLuc string
        +Nome string
        +Descricao string
        +Valor float64
    }

    class HistoricoApolice {
        +ID int
        +ApoliceLuc string
        +Data time.Time
        +Descricao string
        +Ator string
    }

    class Documento {
        +ID int
        +ApoliceLuc string
        +Nome string
        +ArquivoPath string
        +DataAdicao time.Time
        +DeletedAt *time.Time
    }

    class Payload {
        +Luc, Loja, Segmento string
        +Seguradora string
        +Vigencia, Vencimento string
        +Cobertura float64
        +Responsavel string
        +Observacoes string
    }

    class Response {
        +Luc, Loja, Segmento string
        +Seguradora, Status string
        +Vigencia, Vencimento string
        +Cobertura float64
        +DiasRestantes int
        +Responsavel string
        +ResponsavelID *int64
        +CNPJ, NumeroApolice string
    }

    class RenovacaoPayload {
        +NovaVigencia string
        +NovoValor float64
    }

    class ValidationError {
        +Message string
        +Error() string
    }

    class ErrNotFound {
        <<sentinel>>
    }

    class PostgresRepository {
        -db *sql.DB
        +NewRepository(db)
        +implementa Repository
    }

    class Service {
        -repo Repository
        +NewService(repo)
        +List, Get, Create, Update, Delete
        +GetFilaDeAcao, Renovar
        +GetCoberturas, GetHistorico
        -buildModel(payload)
    }

    class Handler {
        -service Service
        -auditSvc audit.Service
        +Collection, Item, FilaDeAcao
        +RenovarApolice, UploadDocumento
        +GetKPI*, Exportar
        -logAudit(...)
        -writeError(...)
    }

    class DomainFunctions {
        <<module>>
        +calculateDaysRemaining
        +calculatePolicyStatus
        +calculateHealthScore
        +ParseDate
        +ToResponse
    }

    PostgresRepository ..|> Repository
    Service --> Repository
    Service ..> Apolice
    Service ..> Payload
    Service ..> DomainFunctions
    Handler --> Service
    Handler ..> Payload
    Handler ..> Response
    Handler ..> RenovacaoPayload
    Handler ..> ValidationError
    Handler ..> ErrNotFound
    Apolice "1" --> "*" Cobertura
    Apolice "1" --> "*" HistoricoApolice
    Apolice "1" --> "*" Documento
```

### Diagrama de sequÃªncia â€” fluxo de renovaÃ§Ã£o de apÃ³lice

```mermaid
sequenceDiagram
    autonumber
    actor Gestor
    participant MW as Middleware
    participant H as Handler
    participant S as Service
    participant R as Repository
    participant A as Audit
    participant DB as PostgreSQL

    Gestor->>MW: POST /renovar (JWT)
    MW->>MW: valida role

    alt invalido
        MW-->>Gestor: 401/403
    end

    MW->>H: chama handler
    H->>H: decodifica payload

    alt JSON invalido
        H-->>Gestor: 400
    end

    H->>S: Renovar(...)
    S->>S: ParseDate

    alt data invalida
        S-->>H: ValidationError
        H-->>Gestor: 400
    end

    S->>R: Renovar(...)
    R->>DB: BEGIN
    R->>DB: UPDATE seguros
    DB-->>R: rows

    alt rows == 0
        R->>DB: ROLLBACK
        R-->>S: ErrNotFound
        S-->>H: ErrNotFound
        H-->>Gestor: 404
    end

    R->>DB: INSERT historico
    R->>DB: COMMIT
    DB-->>R: ok
    R-->>S: ok
    S-->>H: ok

    H->>A: logAudit (async)
    A-->>DB: INSERT audit_logs

    H-->>Gestor: 200 OK
```
