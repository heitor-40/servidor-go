# C4 Model â€” Sistema de Seguros (Grupo-4-Projeto-Integrador)

Documento de arquitetura gerado a partir da leitura direta do cÃ³digo-fonte do projeto, utilizando o modelo C4 (Simon Brown). Todos os diagramas estÃ£o em sintaxe Mermaid, renderizada nativamente pelo GitHub.

---

## SumÃ¡rio

1. [NÃ­vel 1: Contexto](#nÃ­vel-1-contexto)
2. [NÃ­vel 2: ContÃªiner](#nÃ­vel-2-contÃªiner)
3. [NÃ­vel 3: Componente](#nÃ­vel-3-componente)
4. [NÃ­vel 4: CÃ³digo](#nÃ­vel-4-cÃ³digo)
5. [GlossÃ¡rio de tecnologias](#glossÃ¡rio-de-tecnologias)
6. [Modelo de dados](#modelo-de-dados)
7. [Mapeamento de rotas da API](#mapeamento-de-rotas-da-api)
8. [DecisÃµes arquiteturais](#decisÃµes-arquiteturais)

---

## NÃ­vel 1: Contexto

O Diagrama de Contexto mostra o sistema como uma caixa-preta, seus usuÃ¡rios e os sistemas externos com os quais ele se relaciona.

```mermaid
C4Context
    title Sistema de Seguros - CRM Shopping (Nivel 1: Contexto)

    Person(admin, "Administrador", "Gerencia usuarios, acessa audit log completo e tem permissao total sobre apolices")
    Person(gestor, "Gestor", "Cria, edita e renova apolices. Faz upload de documentos e atualiza responsaveis")
    Person(visualizador, "Visualizador / Lojista", "Consulta apolices, dashboards e notificacoes. Apenas leitura")

    System(seguros, "Sistema de Seguros", "CRM para gestao de apolices de seguros do shopping. Gerencia apolices, documentos, notificacoes, audit log e KPIs")

    System_Ext(sinistros, "Sistema de Sinistros", "Plataforma externa para abertura, acompanhamento e regulacao de sinistros vinculados as apolices")
    System_Ext(seguradora, "Seguradora Parceira", "Sistema da seguradora responsavel pela emissao, validacao e cobertura das apolices")
    SystemDb_Ext(bancoExterno, "Banco de Dados Externo", "Base de dados de terceiros (ERP do shopping) consultada para validacao de contratos de locacao")

    Rel(admin, seguros, "Usa", "HTTPS")
    Rel(gestor, seguros, "Usa", "HTTPS")
    Rel(visualizador, seguros, "Usa", "HTTPS")

    Rel(seguros, sinistros, "Consulta e registra sinistros", "JSON/HTTPS")
    Rel(seguros, seguradora, "Valida apolices e coberturas", "JSON/HTTPS")
    Rel(seguros, bancoExterno, "Consulta dados de contratos", "JDBC/HTTPS")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

### Atores

| Ator | Papel |
|---|---|
| Administrador | Gerencia usuÃ¡rios e tem acesso total ao audit log e CRUD de usuÃ¡rios |
| Gestor | Cria, edita, renova apÃ³lices, gerencia documentos e responsÃ¡veis |
| Visualizador / Lojista | Acesso somente leitura: apÃ³lices, dashboards, notificaÃ§Ãµes |

### Sistemas externos

| Sistema | Papel na arquitetura |
|---|---|
| Sistema de Sinistros | Abertura, acompanhamento e regulaÃ§Ã£o de sinistros vinculados Ã s apÃ³lices |
| Seguradora Parceira | EmissÃ£o, validaÃ§Ã£o e cobertura das apÃ³lices |
| Banco de Dados Externo | Base de dados de contratos de locaÃ§Ã£o (ERP do shopping) |

---

## NÃ­vel 2: ContÃªiner

O Diagrama de ContÃªiner detalha as unidades implantÃ¡veis que compÃµem o sistema: aplicaÃ§Ãµes, bancos de dados e serviÃ§os.

```mermaid
C4Container
    title Sistema de Seguros - CRM Shopping (Nivel 2: Conteiner)

    Person(usuario, "Usuario autenticado", "Admin, Gestor ou Visualizador")

    System_Boundary(sistema, "Sistema de Seguros") {
        Container(frontend, "Aplicacao Web (SPA)", "React 19, Vite, TypeScript, TailwindCSS", "Interface do usuario. Rotas: /dashboard, /seguros, /historico, /lojistas, /relatorios, /novo-sinistro. Consome a API via JSON/HTTPS")
        Container(backend, "API Backend", "Go (Golang), net/http stdlib", "Servidor HTTP na porta 8080. Registra e serve todas as rotas /api/*. Valida JWT em cada request protegido. Serve o build estatico do frontend")
        ContainerDb(db, "Banco de Dados", "PostgreSQL 16 (flamboyant_seguros)", "Persiste: usuarios, seguros, coberturas, documentos, historico_apolice, audit_logs, notificacoes")
    }

    System_Ext(sinistros, "Sistema de Sinistros", "Plataforma externa de sinistros")
    System_Ext(seguradora, "Seguradora Parceira", "Emissao e validacao de apolices")
    SystemDb_Ext(bancoExterno, "Banco de Dados Externo", "Contratos de locacao (ERP)")

    Rel(usuario, frontend, "Acessa via browser", "HTTPS, porta 5173 dev / 8080 prod")
    Rel(frontend, backend, "Consome API REST", "JSON/HTTPS, porta 8080, prefixo /api")
    Rel(backend, db, "Le e escreve dados", "SQL/TCP, porta 5432, driver database/sql")

    Rel(backend, sinistros, "Consulta e registra sinistros", "JSON/HTTPS")
    Rel(backend, seguradora, "Valida apolices e coberturas", "JSON/HTTPS")
    Rel(backend, bancoExterno, "Consulta dados de contratos", "JDBC/HTTPS")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

### ContÃªineres internos

| ContÃªiner | Tecnologia | Responsabilidade |
|---|---|---|
| AplicaÃ§Ã£o Web (SPA) | React 19, Vite, TypeScript, TailwindCSS | Interface do usuÃ¡rio acessada pelo navegador. Comunica-se com a API via JSON/HTTPS |
| API Backend | Go (Golang), RESTful | ContÃ©m as regras de negÃ³cio: gestÃ£o de apÃ³lices, sinistros, relatÃ³rios e integraÃ§Ãµes. Orquestra chamadas externas e persiste dados |
| Banco de dados | PostgreSQL 16 | PersistÃªncia relacional de usuÃ¡rios, apÃ³lices, sinistros, contratos vinculados, logs e auditoria |

### ComunicaÃ§Ã£o entre contÃªineres

| Origem | Destino | Protocolo / Detalhe |
|---|---|---|
| UsuÃ¡rio (browser) | AplicaÃ§Ã£o Web (SPA) | HTTPS |
| AplicaÃ§Ã£o Web (SPA) | API Backend | JSON/HTTPS â€” operaÃ§Ãµes de negÃ³cio |
| API Backend | Banco de Dados | SQL/TCP (driver `database/sql`) |
| API Backend | Sistema de Sinistros | JSON/HTTPS â€” API REST externa |
| API Backend | Seguradora Parceira | JSON/HTTPS â€” API REST externa |
| API Backend | Banco de Dados Externo | JDBC/HTTPS â€” consulta de contratos |

---

## NÃ­vel 3: Componente

O Diagrama de Componente detalha os mÃ³dulos internos do contÃªiner **API Backend**, mapeando 1:1 com os pacotes Go do projeto.

```mermaid
C4Component
    title API Backend - Go (Nivel 3: Componente)

    Container_Boundary(backend, "API Backend - Go") {
        Component(main, "cmd/api/main.go", "Go - entrypoint", "Ponto de entrada da aplicacao. Chama app.Run()")
        Component(app, "internal/app/app.go", "Go - compositor", "Carrega config, abre conexao com DB, instancia e conecta todos os pacotes internos. Registra o middleware chain")
        Component(cfg, "pkg/config", "Go - configuracao", "Le variaveis de ambiente: PORT, PG_*, JWT_SECRET, JWT_EXPIRATION_HOURS, FRONTEND_DIR. Valida JWT_SECRET com minimo de 32 caracteres")
        Component(mw, "internal/middleware", "Go - middleware HTTP", "Chain de middlewares: RequestID, Logger, Recover, CORS")
        Component(auth, "internal/auth", "Go - autenticacao e usuarios", "Login, perfil do usuario logado e CRUD de usuarios (admin only). AuthMiddleware valida JWT Bearer. RequireRole valida role")
        Component(apolice, "internal/apolice", "Go - dominio principal", "CRUD de apolices, KPIs, fila de acao, upload/download de documentos, exportacao CSV, busca, map layout e lojas")
        Component(notificacao, "internal/notificacao", "Go - notificacoes", "Lista, marca como lidas e arquiva notificacoes do usuario logado. Tipos: vencida ou a_vencer")
        Component(audit, "internal/audit", "Go - auditoria", "Lista e registra logs de auditoria. Recebe eventos de auth e apolice. Tabela audit_logs com payload anterior e novo em JSONB")
        Component(database, "internal/database/postgres.go", "Go - conexao DB", "Abre e valida conexao com PostgreSQL via database/sql")
        Component(response, "pkg/response", "Go - helpers HTTP", "Padroniza responses JSON: Success e Fail")
    }

    ContainerDb_Ext(db, "PostgreSQL", "flamboyant_seguros")
    System_Ext(sinistros, "Sistema de Sinistros", "Sistema externo")
    System_Ext(seguradora, "Seguradora Parceira", "Sistema externo")

    Rel(main, app, "Chama")
    Rel(app, cfg, "Carrega configuracao")
    Rel(app, database, "Inicializa conexao")
    Rel(app, mw, "Aplica chain de middlewares")
    Rel(app, auth, "Registra rotas, injeta auditSvc e jwtSecret")
    Rel(app, apolice, "Registra rotas, injeta auditSvc e jwtSecret")
    Rel(app, notificacao, "Registra rotas, injeta jwtSecret")
    Rel(app, audit, "Registra rotas, retorna Service compartilhado")
    Rel(auth, audit, "Injeta AuditService - registra login e acoes")
    Rel(apolice, audit, "Injeta AuditService - registra CRUD e uploads")
    Rel(auth, response, "Usa para serializar responses")
    Rel(apolice, response, "Usa para serializar responses")
    Rel(notificacao, response, "Usa para serializar responses")
    Rel(audit, response, "Usa para serializar responses")
    Rel(database, db, "SQL/TCP")
    Rel(apolice, database, "Queries em seguros, coberturas, documentos, historico_apolice")
    Rel(auth, database, "Queries em usuarios")
    Rel(notificacao, database, "Queries em notificacoes JOIN seguros")
    Rel(audit, database, "Queries em audit_logs")
    Rel(apolice, sinistros, "Consulta e registra sinistros", "JSON/HTTPS")
    Rel(apolice, seguradora, "Valida apolices e coberturas", "JSON/HTTPS")

    UpdateLayoutConfig($c4ShapeInRow="4", $c4BoundaryInRow="1")
```

### Componentes do backend

| Componente | Pacote | Responsabilidade |
|---|---|---|
| Entrypoint | `cmd/api/main.go` | Ponto de entrada; chama `app.Run()` |
| Compositor | `internal/app/app.go` | Carrega config, abre DB, instancia e conecta todos os pacotes, registra middlewares |
| ConfiguraÃ§Ã£o | `pkg/config` | LÃª variÃ¡veis de ambiente e valida `JWT_SECRET` |
| Middleware | `internal/middleware` | Chain: RequestID â†’ Logger â†’ Recover â†’ CORS |
| AutenticaÃ§Ã£o | `internal/auth` | Login, perfil do usuÃ¡rio, CRUD de usuÃ¡rios, validaÃ§Ã£o de JWT e roles |
| ApÃ³lices | `internal/apolice` | DomÃ­nio principal: CRUD, KPIs, fila de aÃ§Ã£o, documentos, exportaÃ§Ã£o, busca |
| NotificaÃ§Ãµes | `internal/notificacao` | Lista, marca como lida e arquiva notificaÃ§Ãµes |
| Auditoria | `internal/audit` | Lista e registra aÃ§Ãµes em `audit_logs` (JSONB) |
| ConexÃ£o DB | `internal/database/postgres.go` | Abre e valida conexÃ£o PostgreSQL |
| Response | `pkg/response` | Padroniza responses JSON (`Success`, `Fail`) |

---

## NÃ­vel 4: CÃ³digo

O Diagrama de CÃ³digo detalha as classes (structs, interfaces e funÃ§Ãµes) do pacote de maior complexidade de domÃ­nio: **`internal/apolice`**.

### Diagrama de classes

```mermaid
classDiagram
    direction TB

    class Repository {
        <<interface>>
        +List() []Apolice, error
        +Get(luc string) Apolice, error
        +Create(model Apolice) Apolice, error
        +Update(luc string, model Apolice) Apolice, error
        +Delete(luc string) error
        +SearchApolices(query string) []Apolice, error
        +GetCoberturas(luc string) []Cobertura, error
        +GetHistorico(luc string) []HistoricoApolice, error
        +GetHistoricoGlobal(limit int) []HistoricoApolice, error
        +GetAtividadesRecentes(limit int) []AtividadeRecente, error
        +UpdateObservacoes(luc string, obs string) error
        +Renovar(luc string, novoVencimento Time, novoValor float64, ator string, descricao string) error
        +UpdateResponsavel(luc string, responsavelID int64, ator string) error
        +GetLojas() []LojaInfo, error
        +GetUsuarios() []Usuario, error
        +GetDocumentoByID(id string) Documento, error
        +GetDocumentosByApolice(luc string) []Documento, error
        +CreateDocumento(doc Documento) Documento, error
        +DeleteDocumento(id string) error
    }

    class Apolice {
        +Luc string
        +Loja string
        +Segmento string
        +Seguradora string
        +Vigencia time.Time
        +Vencimento time.Time
        +Status string
        +Cobertura float64
        +DiasRestantes int
        +Responsavel string
        +ResponsavelID *int64
        +Observacoes string
        +CNPJ string
        +NumeroApolice string
    }

    class Cobertura {
        +ID int
        +ApoliceLuc string
        +Nome string
        +Descricao string
        +Valor float64
    }

    class HistoricoApolice {
        +ID int
        +ApoliceLuc string
        +Data time.Time
        +Descricao string
        +Ator string
    }

    class Documento {
        +ID int
        +ApoliceLuc string
        +Nome string
        +ArquivoPath string
        +DataAdicao time.Time
        +DeletedAt *time.Time
    }

    class AtividadeRecente {
        +ID string
        +Luc string
        +NomeLoja string
        +Acao string
        +Responsavel string
        +Timestamp time.Time
    }

    class LojaInfo {
        +Luc string
        +Nome string
        +Segmento string
    }

    class Payload {
        +Luc string
        +Loja string
        +Segmento string
        +Seguradora string
        +Vigencia string
        +Vencimento string
        +Cobertura float64
        +Responsavel string
        +Observacoes string
    }

    class Response {
        +Luc string
        +Loja string
        +Segmento string
        +Seguradora string
        +Vigencia string
        +Vencimento string
        +Status string
        +Cobertura float64
        +DiasRestantes int
        +Responsavel string
        +ResponsavelID *int64
        +Observacoes string
        +CNPJ string
        +NumeroApolice string
    }

    class RenovacaoPayload {
        +NovaVigencia string
        +NovoValor float64
    }

    class UpdateResponsavelPayload {
        +ResponsavelID int64
    }

    class ValidationError {
        +Message string
        +Error() string
    }

    class ErrNotFound {
        <<sentinel>>
        apolice nao encontrada
    }

    class PostgresRepository {
        -db *sql.DB
        +NewRepository(db *sql.DB) PostgresRepository
        +List() []Apolice, error
        +Get(luc string) Apolice, error
        +Create(model Apolice) Apolice, error
        +Update(luc string, model Apolice) Apolice, error
        +Delete(luc string) error
        +SearchApolices(query string) []Apolice, error
        +GetCoberturas(luc string) []Cobertura, error
        +GetHistorico(luc string) []HistoricoApolice, error
        +GetHistoricoGlobal(limit int) []HistoricoApolice, error
        +GetAtividadesRecentes(limit int) []AtividadeRecente, error
        +UpdateObservacoes(luc string, obs string) error
        +Renovar(...) error
        +UpdateResponsavel(...) error
        +GetLojas() []LojaInfo, error
        +GetUsuarios() []Usuario, error
        +GetDocumentoByID(id string) Documento, error
        +GetDocumentosByApolice(luc string) []Documento, error
        +CreateDocumento(doc Documento) Documento, error
        +DeleteDocumento(id string) error
        -insertHistoricoTx(tx, luc, descricao, ator) error
        -scanApolice(scanner) Apolice, error
    }

    class Service {
        -repo Repository
        +NewService(repo Repository) Service
        +List() []Apolice, error
        +SearchApolices(query string) []Apolice, error
        +Get(luc string) Apolice, error
        +Create(payload Payload) Apolice, error
        +Update(luc string, payload Payload) Apolice, error
        +Delete(luc string) error
        +GetFilaDeAcao() []Apolice, error
        +GetCoberturas(luc string) []Cobertura, error
        +GetHistorico(luc string) []HistoricoApolice, error
        +GetHistoricoGlobal(limit int) []HistoricoApolice, error
        +GetAtividadesRecentes(limit int) []AtividadeRecente, error
        +UpdateObservacoes(luc string, obs string) error
        +Renovar(luc, novaVigencia string, novoValor float64, ator string) error
        +GetLojas() []LojaInfo, error
        +GetDocumentoByID(id string) Documento, error
        +GetDocumentosByApolice(luc string) []Documento, error
        +CreateDocumento(doc Documento) Documento, error
        +DeleteDocumento(id string) error
        -buildModel(payload Payload) Apolice, error
    }

    class Handler {
        -service Service
        -auditSvc audit.Service
        +NewHandler(service, auditSvc) Handler
        +Collection(w, r)
        +Item(routePrefix string) http.HandlerFunc
        +FilaDeAcao(w, r)
        +SearchApolices(w, r)
        +GetCoberturas(w, r)
        +GetHistorico(w, r)
        +GetAtividadesRecentes(w, r)
        +GetKPIHistory(w, r)
        +GetExpiringByWeek(w, r)
        +GetCoverageHistory(w, r)
        +GetRiskBySegment(w, r)
        +GetHealthScore(w, r)
        +UpdateObservacoes(w, r)
        +UpdateApoliceResponsavel(w, r)
        +RenovarApolice(w, r)
        +GetMapLayout(w, r)
        +GetLojas(w, r)
        +GetDocumentos(w, r)
        +UploadDocumento(w, r)
        +DownloadDocumento(w, r)
        +DeleteDocumento(w, r)
        +Exportar(w, r)
        -logAudit(r, acao, entidade, entidadeID, anterior, novo)
        -writeError(w, requestID, err)
    }

    class DomainFunctions {
        <<module>>
        +calculateDaysRemaining(vencimento time.Time) int
        +calculatePolicyStatus(vencimento time.Time) string
        +calculateHealthScore(items []Apolice, at time.Time) int
        +buildKPIHistoryPoints(items []Apolice, weeks int, metric string) []KPIHistoryPoint
        +countConformesAt(items []Apolice, at time.Time) int
        +countVencidasAt(items []Apolice, at time.Time) int
        +ParseDate(value string) time.Time, error
        +ToResponse(model Apolice) Response
        +toJSON(v any) string
    }

    PostgresRepository ..|> Repository : implementa
    Service --> Repository : depende da interface
    Service ..> Apolice : produz e consome
    Service ..> Payload : recebe como entrada
    Service ..> DomainFunctions : usa calculateDaysRemaining e calculatePolicyStatus
    Handler --> Service : chama
    Handler ..> Payload : decodifica do body HTTP
    Handler ..> Response : retorna via ToResponse
    Handler ..> RenovacaoPayload : decodifica do body HTTP
    Handler ..> UpdateResponsavelPayload : decodifica do body HTTP
    Handler ..> ValidationError : trata via writeError
    Handler ..> ErrNotFound : trata via writeError
```

### Diagrama de sequÃªncia â€” fluxo de renovaÃ§Ã£o de apÃ³lice

```mermaid
sequenceDiagram
    autonumber
    actor Gestor
    participant MW as RequireRole Middleware
    participant H as Handler
    participant S as Service
    participant R as PostgresRepository
    participant A as audit.Service
    participant DB as PostgreSQL

    Gestor->>MW: POST /api/apolices/{id}/renovar (Bearer JWT)
    MW->>MW: Valida JWT HS256 e verifica role admin ou gestor

    alt Token invalido ou role insuficiente
        MW-->>Gestor: 401 / 403
    end

    MW->>H: HandlerFunc chamado
    H->>H: Decodifica RenovacaoPayload (nova_vigencia, novo_valor)

    alt JSON invalido
        H-->>Gestor: 400 Bad Request
    end

    H->>S: Renovar(luc, novaVigencia, novoValor, ator)
    S->>S: ParseDate(novaVigencia)

    alt Data invalida
        S-->>H: ValidationError
        H-->>Gestor: 400 Bad Request
    end

    S->>R: Renovar(luc, novoVencimento, novoValor, ator, descricao)
    R->>DB: BEGIN TRANSACTION
    R->>DB: UPDATE seguros SET vencimento, cobertura WHERE luc
    DB-->>R: rowsAffected

    alt rowsAffected igual a zero
        R->>DB: ROLLBACK
        R-->>S: ErrNotFound
        S-->>H: ErrNotFound
        H-->>Gestor: 404 Not Found
    end

    R->>DB: INSERT INTO historico_apolice
    R->>DB: COMMIT
    DB-->>R: OK
    R-->>S: sucesso
    S-->>H: sucesso

    H->>A: logAudit (acao renovar, entidade apolice)
    note over A,DB: goroutine assincrona, nao bloqueia a resposta
    A-->>DB: INSERT INTO audit_logs

    H-->>Gestor: 200 OK
```

---

## GlossÃ¡rio de tecnologias

| Camada | Tecnologia | ObservaÃ§Ã£o |
|---|---|---|
| Frontend | React 19 + Vite + TypeScript + TailwindCSS | SPA com tipagem estÃ¡tica e build rÃ¡pido |
| Backend | Go (Golang), `net/http` stdlib | API RESTful, sem framework externo |
| AutenticaÃ§Ã£o | JWT (HS256) | Validado em `AuthMiddleware`; roles: admin, gestor, visualizador |
| Banco de dados | PostgreSQL 16 | PersistÃªncia relacional com transaÃ§Ãµes ACID |
| Infraestrutura | Docker Compose | OrquestraÃ§Ã£o local dos serviÃ§os `flamboyant_web`, `flamboyant_api`, `flamboyant_db` |
| Sistemas externos | Sistema de Sinistros, Seguradora Parceira, Banco de Dados Externo (ERP) | IntegraÃ§Ãµes previstas na arquitetura |

---

## Modelo de dados

ExtraÃ­do de `migrations/init/01_schema.sql`:

| Tabela | Chave principal | DescriÃ§Ã£o |
|---|---|---|
| `usuarios` | `id SERIAL` | UsuÃ¡rios do sistema, com `CHECK` de role: admin, gestor, visualizador |
| `seguros` | `luc VARCHAR` | ApÃ³lices de seguro â€” entidade central do domÃ­nio |
| `coberturas` | `id SERIAL` | Coberturas vinculadas a uma apÃ³lice (`apolice_luc`) |
| `documentos` | `id SERIAL` | Arquivos PDF/imagem vinculados a uma apÃ³lice, com soft delete (`deleted_at`) |
| `historico_apolice` | `id SERIAL` | Timeline de eventos de cada apÃ³lice (criaÃ§Ã£o, renovaÃ§Ã£o, ediÃ§Ã£o) |
| `audit_logs` | `id BIGSERIAL` | Log imutÃ¡vel de aÃ§Ãµes, com payload anterior/novo em JSONB |
| `notificacoes` | `id SERIAL` | Alertas de apÃ³lices vencidas ou a vencer, por usuÃ¡rio |

---

## Mapeamento de rotas da API

ExtraÃ­do de `internal/apolice/routes.go`, `internal/auth/routes.go`, `internal/notificacao/routes.go` e `internal/audit/routes.go`.

| MÃ©todo | Rota | Role exigida | Handler |
|---|---|---|---|
| POST | `/api/auth/login` | pÃºblico | `auth.Login` |
| GET | `/api/auth/me` | autenticado | `auth.Me` |
| GET / POST / PATCH | `/api/usuarios` | admin | `auth` CRUD de usuÃ¡rios |
| GET | `/api/apolices` | autenticado | `apolice.Collection` |
| POST | `/api/apolices` | admin, gestor | `apolice.Collection` |
| GET / PUT / PATCH | `/api/apolices/{id}` | autenticado / admin+gestor (escrita) | `apolice.Item` |
| DELETE | `/api/apolices/{id}` | admin | `apolice.Item` |
| PATCH | `/api/apolices/{id}/observacoes` | admin, gestor | `apolice.UpdateObservacoes` |
| PATCH | `/api/apolices/{id}/responsavel` | admin, gestor | `apolice.UpdateApoliceResponsavel` |
| POST | `/api/apolices/{id}/renovar` | admin, gestor | `apolice.RenovarApolice` |
| GET | `/api/apolices/{id}/coberturas` | autenticado | `apolice.GetCoberturas` |
| GET | `/api/apolices/{id}/historico` | autenticado | `apolice.GetHistorico` |
| GET | `/api/apolices/{id}/documentos` | autenticado | `apolice.GetDocumentos` |
| POST | `/api/apolices/{id}/documentos` | admin, gestor | `apolice.UploadDocumento` |
| GET | `/api/documentos/{id}/download` | autenticado | `apolice.DownloadDocumento` |
| DELETE | `/api/documentos/{id}` | admin, gestor | `apolice.DeleteDocumento` |
| GET | `/api/apolices/atividade-recente` | autenticado | `apolice.GetAtividadesRecentes` |
| GET | `/api/apolices/exportar` | autenticado | `apolice.Exportar` |
| GET | `/api/apolices/search` | autenticado | `apolice.SearchApolices` |
| GET | `/api/fila-de-acao` | autenticado | `apolice.FilaDeAcao` |
| GET | `/api/kpis/history` | autenticado | `apolice.GetKPIHistory` |
| GET | `/api/kpis/expiring-by-week` | autenticado | `apolice.GetExpiringByWeek` |
| GET | `/api/kpis/coverage-history` | autenticado | `apolice.GetCoverageHistory` |
| GET | `/api/kpis/risk-by-segment` | autenticado | `apolice.GetRiskBySegment` |
| GET | `/api/kpis/health-score` | autenticado | `apolice.GetHealthScore` |
| GET | `/api/map-layout` | autenticado | `apolice.GetMapLayout` |
| GET | `/api/lojas` | autenticado | `apolice.GetLojas` |
| GET | `/api/notificacoes` | autenticado | `notificacao.List` |
| PATCH | `/api/notificacoes/marcar-lidas` | autenticado | `notificacao.MarcarLidas` |
| DELETE | `/api/notificacoes/arquivadas` e `/api/notificacoes/{id}` | autenticado | `notificacao.Delete` |
| GET | `/api/admin/audit` | admin | `audit.List` |
| POST | `/api/admin/audit` | admin | `audit.Create` |

---

## DecisÃµes arquiteturais

| DecisÃ£o | Justificativa |
|---|---|
| Go com `net/http` stdlib, sem framework | Reduz dependÃªncias externas; suficiente para o volume de rotas do projeto |
| Interface `Repository` em `internal/apolice` | Inverte a dependÃªncia entre `Service` e `PostgresRepository`, facilitando testes com mocks |
| Auditoria injetada via `audit.Service` compartilhado | Centraliza o log de aÃ§Ãµes em uma Ãºnica tabela (`audit_logs`) sem acoplar os pacotes de domÃ­nio entre si |
| TransaÃ§Ãµes SQL em operaÃ§Ãµes de mÃºltiplas tabelas (ex: renovaÃ§Ã£o) | Garante atomicidade entre `UPDATE seguros` e `INSERT historico_apolice` |
| JWT (HS256) com roles embutidas | Evita chamada adicional ao banco para checagem de permissÃ£o em cada request |
| Frontend servido pelo prÃ³prio backend em produÃ§Ã£o | Simplifica o deploy â€” um Ãºnico binÃ¡rio Go serve API e assets estÃ¡ticos |
| PostgreSQL com schema Ãºnico (`flamboyant_seguros`) | Modelo relacional adequado ao domÃ­nio de apÃ³lices, coberturas e histÃ³rico, com integridade referencial |
| Sistemas externos (Sinistros, Seguradora, ERP) representados como `System_Ext` | Embora nÃ£o implementados no cÃ³digo atual, fazem parte do domÃ­nio de negÃ³cio e da evoluÃ§Ã£o planejada da arquitetura |
